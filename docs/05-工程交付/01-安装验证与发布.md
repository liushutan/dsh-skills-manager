# 安装、验证与发布

更新时间：2026-08-17 15:00（Asia/Shanghai）

## 安装

当前发布版本：[`@liushutan/dsh-skills-manager@0.1.29`](https://www.npmjs.com/package/@liushutan/dsh-skills-manager)。推荐直接安装，无需下载源码：

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
dsh plugin --profile web add @liushutan/dsh-skills-manager
```

重启 DSH 后在「设置 → 技能」确认插件界面已更新。若仍显示旧界面，先确认活动 profile 已重新加载该包。若下载镜像尚未同步最新包，可在安装命令末尾添加 `--registry=https://registry.npmjs.org/`。

本地开发或未发布版本可在插件根目录执行 `dsh plugin --profile web add .`。

## 发布新版本

受信发布绑定完成后，维护者只需在插件根目录升级版本并推送版本标签：

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
npm version patch
npm run pack:check
git push origin master --follow-tags
```

`.github\workflows\publish.yml` 会在推送 `v*` 版本标签时执行，也支持手动触发；它会通过 Corepack 按 `pnpm-lock.yaml` 安装依赖、运行测试，再通过 npm Trusted Publishing（OIDC）发布到官方 npm registry。该方式不需要将 npm 写入令牌保存到 GitHub。

首次启用前，需在 npm 包设置的「Trusted Publisher」中绑定 `MichengAI/dsh-skills-manager` 和工作流文件 `publish.yml`。若自动发布故障，可在完成本机身份验证后执行 `npm publish` 作为人工回退。

包内 `publishConfig.registry` 会强制指向官方 npm registry。发布后，重新执行安装命令即可获取指定版本；回滚时安装已验证版本号，例如 `dsh plugin --profile web add @liushutan/dsh-skills-manager@0.1.3`。

## 自动验证

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
node --check lib\core.js
node --check lib\index.js
node --check lib\client.js
node test\core-test.mjs
```

## 端到端验收

1. DSH 分组与公共 Agent 分组均可见。
2. 仅 DSH 分组能启用和停用技能；公共 Agent 分组没有修改按钮。
3. 可向 DSH 分组上传 `SKILL.md` 所在目录；同名时需要确认覆盖。
4. 仅 DSH 分组可删除，公共 Agent 分组不出现删除入口。
5. 按 ESC 时只关闭最上层确认框或上传框。

## 回滚

从 DSH Web profile 移除当前插件或恢复上一个已验证版本，然后重启 DSH。插件不会修改公共 Agent 目录中的文件结构；DSH 根目录内被覆盖或删除的技能需由安装前备份恢复。
