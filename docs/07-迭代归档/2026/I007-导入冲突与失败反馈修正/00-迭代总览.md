# I007-导入冲突与失败反馈修正 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-15 17:01（Asia/Shanghai）

## 目标与业务边界

修复导入流程中同名但不同条目形态绕过覆盖确认、以及失败导入被显示为成功的问题。仅改变导入结果判定与展示，不扩大公共 Agent 目录权限。

## 任务清单

- [x] 以规范化技能名检查 bundle 与 flat 两种目标路径。
- [x] 覆盖时删除该名称的现有两种形态，只写入新形态。
- [x] 全部失败时返回错误；部分失败时客户端显示成功和失败明细。
- [x] 补充回归测试并更新有效规范。

## 验收标准

- [x] bundle/flat 跨形态同名会进入冲突确认。
- [x] 跨形态覆盖后不保留旧条目。
- [x] 单条非法名称导入返回错误。
- [x] `node test\core-test.mjs`：55 项通过。

## 已完成内容

导入以规范化后的技能名为唯一键。批量导入仍允许部分成功，但界面会同时报告成功条目与失败原因；没有任何成功条目时，接口返回失败。

## 待处理事项

真实 DSH profile 的重新加载与端到端验证仍见交接入口待办。

## 关联提交

待提交。

## 阅读顺序

1. 本文件
2. `docs\03-技术架构\01-技能存储与规范.md`
3. `test\core-test.mjs`
