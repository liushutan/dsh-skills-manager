# I004-上传后备与菜单排序修正 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU
- 优先级：中
- 更新时间：2026-08-15 16:41（Asia/Shanghai）

## 目标与业务边界

处理评审发现：菜单排序与插件分区一致；上传不再只依赖 Electron 的 `File.path`，并核对已不存在的 `rank` 字段。

## 任务清单

- [x] 将 `settings.section` 的 `order` 统一为 `17`。
- [x] 注入 DSH `workspaces` 服务，并接入 `pickDirectory()` 上传后备路径。
- [x] 确认 `state()` 未引用或返回已移除的 `rank` 字段。
- [x] 添加客户端静态约束测试与更新文档。

## 验收标准

- [x] 三个 JS 文件通过 `node --check`。
- [x] `node test\core-test.mjs`：44 项通过。
- [ ] 在真实 DSH 客户端验证文件与目录两条选择路径。

## 已完成内容

默认原生文件选择保持不变；仅在文件路径不可用时，使用 DSH 原生目录选择器返回插件目录。

## 待处理事项

真实客户端端到端验证已移入交接入口待办。

## 关联提交

暂无（工作目录不是 Git 仓库）。

## 阅读顺序

1. 本文件
2. `docs\03-技术架构\01-技能存储与规范.md`
3. `docs\03-技术架构\02-HTTP接口文档.md`
