# I002-DSH单目录管理与界面优化 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-15 15:14（Asia/Shanghai）

## 目标与业务边界

将技能管理收敛到 DSH 目录，移除检查功能与 Agent 根目录管理；提供原生文件选择上传、删除确认和更清晰的深色设置页。

边界：仅处理 `$DSH_HOME\skills`；不实现 zip 解压、在线编辑或项目级技能管理。

## 任务清单

- [x] 移除检查、自动修复和目录浏览接口。
- [x] 移除 `.agents\skills` 根目录和上传目标选择。
- [x] 支持选择 `SKILL.md` 后整体导入父目录，并处理同名覆盖。
- [x] 增加 DSH 插件删除确认与服务端删除接口。
- [x] 重做设置页工具栏、统计、列表与上传弹窗。
- [x] 更新单元测试、README 与当前架构文档。

## 验收标准

- [x] `node --check lib\core.js`、`lib\index.js`、`lib\client.js` 通过。
- [x] `node test\core-test.mjs`：35 项通过。
- [ ] 在真实 DSH 桌面客户端完成上传、覆盖、启停和删除验证。

## 已完成内容

代码与当前有效文档已更新；旧版检查和双目录管理能力已从产品路径移除。

## 待处理事项

真实 DSH 客户端端到端验证已移入交接入口待办。

## 关联提交

暂无（工作目录不是 Git 仓库）。

## 阅读顺序

1. 本文件
2. `docs\03-技术架构\01-技能存储与规范.md`
3. `docs\03-技术架构\02-HTTP接口文档.md`
