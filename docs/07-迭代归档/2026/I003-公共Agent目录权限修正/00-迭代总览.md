# I003-公共Agent目录权限修正 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-15 16:36（Asia/Shanghai）

## 目标与业务边界

纠正公共 Agent 技能目录权限：恢复列表与启用/停用，但禁止上传、覆盖和删除。DSH 根目录保留完整管理能力。

## 任务清单

- [x] 恢复 `$DSH_AGENTS_HOME\skills` 扫描与状态展示。
- [x] 允许公共 Agent 技能启用/停用。
- [x] 前端仅在 DSH 分组显示删除按钮。
- [x] 服务端拒绝公共 Agent 技能删除；导入目标仍固定为 DSH。
- [x] 更新单元测试与当前有效文档。

## 验收标准

- [x] 三个 JS 文件通过 `node --check`。
- [x] `node test\core-test.mjs`：40 项通过。
- [ ] 在真实 DSH 客户端验证公共目录仅可启停。

## 已完成内容

公共目录按最小权限展示；破坏性操作仅保留在 DSH 目录。

## 待处理事项

真实客户端端到端验证已移入交接入口待办。

## 关联提交

暂无（工作目录不是 Git 仓库）。

## 阅读顺序

1. 本文件
2. `docs\03-技术架构\01-技能存储与规范.md`
3. `docs\03-技术架构\02-HTTP接口文档.md`
