# I001-技能管理插件初版 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-15 14:03（Asia/Shanghai）

## 目标与业务边界

实现 DSH Web 插件 `dsh-skills-manager` 初版，在设置侧边栏提供「Skill 管理」面板，覆盖：技能列表、启用/停用、规范检查与自动修复、导入。

边界：只管理用户级两个技能根（`~/.dsh/skills`、`~/.agents/skills`）；不碰项目级；不自动修复；不含创建/编辑能力。

## 任务清单

- [x] 需求评审与参考实现调研（`dsh-codex-ui`、`dsh-skill-manager-ytxue`）
- [x] 技术方案定稿（双根、frontmatter 开关、状态驱动审计）
- [x] `lib/core.js` 核心（扫描/解析/校验/修复/启停/导入）
- [x] `lib/index.js` host 半区（webServer 路由）
- [x] `lib/client.js` 浏览器半区（settings.section 面板）
- [x] `test/core-test.mjs` 单元测试（49 项通过）
- [x] `package.json` / `cordis.patch.yml` / `README.md`

## 阻塞事项

无。

## 验收标准

- [x] 核心单测 `node test/core-test.mjs` 49/49 通过。
- [x] 三个 JS 文件通过 `node --check` 语法检查。
- [ ] 本机安装 + 浏览器端到端验证（遗留，见待办）。

## 已完成内容

第一版全部代码与文档（见任务清单）。

## 待处理事项

- 本机安装与端到端验证（转入 `docs\00-交接入口\03-待办与阻塞.md`）。
- 创建/编辑能力（规划为 I002）。

## 关联提交

暂无（项目尚未初始化 git 仓库）。

## 阅读顺序

1. 本文件
2. `docs\03-技术架构\01-技能存储与规范.md`
3. `docs\03-技术架构\02-HTTP接口文档.md`
