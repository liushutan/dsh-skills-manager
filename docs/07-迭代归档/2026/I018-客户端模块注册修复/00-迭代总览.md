# I018-客户端模块注册修复

状态：已完成  
负责人：Codex / YUJIYU  
优先级：高  
更新时间：2026-08-15 21:10（Asia/Shanghai）

## 目标与业务边界

修复 npm scope 与客户端 `ModuleLoader` 注册 ID 不一致造成的插件无法加载问题。

## 已完成内容

- 将客户端 bundle 注册 ID 从 `dsh-skills-manager` 更正为 `@michengai/dsh-skills-manager`。
- 增加 scoped 模块 ID 回归断言。

## 验收标准与结果

- `node test\core-test.mjs`：87 passed, 0 failed。
- `npm run pack:check`、`node --check lib\client.js`、`git diff --check`：通过。

## 关联提交

待提交。
