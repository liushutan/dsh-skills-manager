# I023-斜杠菜单即时刷新 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU / Codex
- 优先级：高
- 更新时间：2026-08-16 17:25（Asia/Shanghai）

## 目标与业务边界

启停、安装、删除技能后，聊天输入框的 `/` 列表应立刻更新，不必按 F5。不修改官方 `dsh-client-ui-skill`，不伪造会话预设。

## 任务清单

- [x] 写明根因：宿主 `skills/change` 未进入 Web 转发白名单，`/` 菜单按会话缓存 `skills.list`。
- [x] 成功写入后继续失效宿主技能目录。
- [x] 对带预设的活跃会话补发已转发的 `agent-preset/selected`，并发送 `commands/change`。
- [x] 没有预设的会话不发事件，避免改写会话芯片。
- [x] 补充 HTTP 与直接通知测试。

## 阻塞事项

无。

## 验收标准

- [x] `node test\core-test.mjs`：142 项通过。
- [x] `node test\locale-test.mjs`：449 项通过。
- [ ] 真实 DSH 客户端中启停或安装后，不按 F5 即可在 `/` 中看到变化。

## 已完成内容

写入成功后通知宿主目录与 Web `/` 缓存刷新。

## 待处理事项

真实客户端验收转入交接入口待办。

## 关联提交

- 斜杠菜单即时刷新与 `0.1.12` 发布提交

## 阅读顺序

1. 本文件
2. `docs\00-交接入口\05-技术架构基线.md`
3. `lib\index.js`
