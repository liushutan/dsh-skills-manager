# I017-导入边界与状态一致性修复

状态：已完成  
负责人：Codex / YUJIYU  
优先级：高  
更新时间：2026-08-15 20:50（Asia/Shanghai）

## 目标与业务边界

修复解析 `SKILL.md` 后导入来源变化导致的重叠校验遗漏，并消除剩余的低风险路径、扫描和状态展示不一致；不增加技能规范检查功能。

## 已完成内容

- 重叠校验改为使用实际复制来源 `analysis.source`，拒绝选择 `$DSH_HOME\SKILL.md` 后间接导入整个 DSH 主目录。
- 增加直接删除和 HTTP 删除对 `.`、嵌套路径的回归保护，并验证被拒后技能仍存在。
- DSH 可写根按 `key: dsh` 查找，拒绝 Windows 保留设备名。
- 符号链接检查改为迭代遍历并限制来源目录深度为 64 层。
- 扫描支持大小写混合的 `.MD` 扩展名；非法调用策略显示为“配置异常”，可通过“修复并启用”恢复。
- 移除仅测试使用的 YAML 序列化死代码。

## 验收标准与结果

- `node test\core-test.mjs`：86 passed, 0 failed。
- `node --check lib\core.js`、`node --check lib\client.js`：通过。
- `git diff --check`：通过。

## 遗留事项

- 真实 DSH 客户端端到端验证仍待完成。

## 关联提交

`40aaa73 fix: close skills import edge cases`
