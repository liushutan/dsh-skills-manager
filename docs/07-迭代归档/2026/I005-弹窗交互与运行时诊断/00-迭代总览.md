# I005-弹窗交互与运行时诊断 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-15 16:51（Asia/Shanghai）

## 目标与业务边界

修复上传弹窗的 ESC 关闭层级与选取文件无反馈问题，并核实公共 `$DSH_AGENTS_HOME\skills` 的展示与启停能力。公共目录仍不允许上传、覆盖或删除。

## 任务清单

- [x] 在捕获阶段处理 `Escape`，只关闭当前最上层弹窗并阻止设置页继续处理事件。
- [x] 在上传弹窗内显示原生目录选择中与选择失败状态。
- [x] 校验当前源码读取到 DSH 与公共 Agent 两个技能根目录。
- [x] 补充静态约束测试与更新交接文档。

## 验收标准

- [x] 三个 JS 文件通过 `node --check`。
- [x] `node test\core-test.mjs`：46 项通过。
- [x] 源码直接读取状态时识别到 23 个公共 Agent 技能。
- [ ] 在重新加载后的真实 DSH 客户端验证上传、ESC 与双目录列表。

## 已完成内容

上传框会在自身内展示选择过程或失败原因；嵌套确认框、上传框按 ESC 从内到外依次关闭。当前源码直接读取状态时，公共 Agent 根目录存在且包含 23 个技能；用户截图所示运行时不是当前源码加载的版本。

## 待处理事项

真实运行时的 profile 未安装本插件，需要重新安装或重新加载后做端到端验证；该事项已移入交接入口待办。

## 关联提交

暂无（工作目录不是 Git 仓库）。

## 阅读顺序

1. 本文件
2. `docs\00-交接入口\02-当前状态.md`
3. `docs\03-技术架构\01-技能存储与规范.md`
