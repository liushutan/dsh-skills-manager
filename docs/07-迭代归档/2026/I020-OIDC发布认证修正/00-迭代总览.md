# I020-OIDC发布认证修正

状态：已完成  
负责人：Codex / YUJIYU  
优先级：高  
更新时间：2026-08-15 20:29（Asia/Shanghai）

## 目标与业务边界

完成 npm Trusted Publishing 与 GitHub Actions 的真实发布验证；发布至官方 npm registry，不发布至 GitHub Packages。

## 已完成内容

- 在 npm 包设置中建立 `MichengAI/dsh-skills-manager` 与 `publish.yml` 的 GitHub Actions 受信发布连接，仅允许 `npm publish`。
- 将 npm 发布权限切换为“要求 2FA 且禁止 bypass 2FA 令牌”。
- 移除 `actions/setup-node` 的 `registry-url`，复用包内 `publishConfig.registry`，避免生成令牌式认证配置。
- 新增工作流不含 `registry-url` 的回归断言。
- 重跑 `v0.1.5`，GitHub Actions 成功发布 npm 包。

## 验收标准与结果

- `npm test`：91 passed, 0 failed。
- `npm run pack:check`、`git diff --check`：通过。
- GitHub Actions 运行 `31884515005`：success。
- npm 官方仓库：`@michengai/dsh-skills-manager@0.1.5`，`latest` 为 `0.1.5`。

## 关联提交

- `b26456f fix: use OIDC publishing without token config`

## 遗留事项

- 后续仅需升级版本并推送 `v*` 标签；工作流将自动测试和发布。
