# I032 修复并启用字段契约修复

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-27 00:08（Asia/Shanghai）

## 目标与业务边界

修复状态接口漏传 `hasFrontmatter` 导致“修复并启用”入口永远不显示的问题；不改变客户端交互或技能文件格式。

## 任务清单

- 先补充非法调用策略技能的状态快照失败测试。
- 在 `state()` 技能项中下发 `hasFrontmatter`。
- 运行全量测试并做本地 DSH Web 验证。

## 阻塞事项

无。

## 验收标准

- 完整 frontmatter 且调用策略非法的技能返回 `hasFrontmatter: true`。
- 同一技能保持 `invocationPolicyValid: false` 与 `loadable: false`。
- 客户端能够满足 `repairable` 条件并显示“修复并启用”。

## 已完成内容

- 状态快照字段契约与运行时回归测试已补齐。

## 待处理事项

无。

## 关联提交

- 本次修复提交（见 Git 历史）。

## 阅读顺序

1. 本文。
2. `docs\03-技术架构\02-HTTP接口文档.md`。
