# I010-frontmatter完整性与目录刷新 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-15 17:26（Asia/Shanghai）

## 目标与业务边界

修复启停时反复转义其他 frontmatter 字段、YAML 块文本显示为特殊字符，以及写入后 `/` 候选仍使用旧缓存的问题。公共 Agent 目录调整为只读，不再改写其全局元数据。

## 任务清单

- [x] DSH 技能启停改为原文级替换调用策略行。
- [x] 支持读取带引号与 `|`/`>-` 的描述值。
- [x] 写入后使 DSH 技能目录缓存立即失效。
- [x] 公共 Agent 技能改为前后端均不可修改。
- [x] 恢复本机 `humanizer-zh` 已损坏的公开 frontmatter 和默认调用策略。
- [x] 补充回归测试和更新有效文档。

## 验收标准

- [x] 引号和嵌套 frontmatter 经 DSH 启停后保持字节内容不变。
- [x] YAML 折叠块描述显示为正常文本。
- [x] 公共 Agent 技能启停请求被拒绝。
- [x] `node test\core-test.mjs`：62 项通过。

## 已完成内容

启停现在只修改 DSH 技能的两个调用策略字段，避免任何元数据反复转义。公共 Agent 技能只读；本机 `humanizer-zh` 已恢复原始公开元数据和默认调用策略。

## 待处理事项

重新加载最新插件后，确认公共 Agent 技能没有操作按钮，并验证 DSH 技能启停和 `/` 候选刷新。

## 关联提交

`79c446d fix: preserve skill metadata and make Agent skills read-only`

## 阅读顺序

1. 本文件
2. `lib\core.js`
3. `lib\index.js`
