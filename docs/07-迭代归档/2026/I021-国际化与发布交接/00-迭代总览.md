# I021-国际化与发布交接 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU / Codex
- 优先级：高
- 更新时间：2026-08-15 23:54（Asia/Shanghai）

## 目标与业务边界

接入 DSH `locale` 服务，把硬编码中文界面改为 zh/en 双语；业务错误码化并收口国际化文档与版本发布交接。不增加新功能，不改变现有中文行为。

## 任务清单

- [x] Client 词典化：zh/en 双语词典（各 79 key），组件经 `props.t` 翻译，`label` 用 thunk 跟随语言切换。
- [x] host/core 业务错误码化：12 个 `error.*` 码 + `params`，保留 `error` 中文兜底；`readonlyError()` 去重。
- [x] 协议错误码化：`error.proto.*` 6 码（403/415/405/404/413/400），UI 不再漏出英文原文。
- [x] 状态不冻译文：本地错误存 `code`、目录选择存 `nameKey`，渲染时再翻译，切语言即时生效。
- [x] 加固：`translateError`/`translateOrFallback` 对 null/undefined 回退；`attachCode` 仅接受 `error.*` 前缀。
- [x] 测试：locale-test 补 translateError 用例与注册契约断言（inject 含 locale、`locale: NS`）。
- [x] 文档：接口文档错误码约定、架构基线 locale 依赖、当前状态/待办/路线图更新，发布 `0.1.6`。

## 阻塞事项

无。

## 验收标准

- [x] `node --check` lib/core、index、client 通过。
- [x] `node test\core-test.mjs`：117 项通过。
- [x] `node test\locale-test.mjs`：416 项通过（key/占位符对齐 + translateError + 注册契约）。
- [x] `@michengai/dsh-skills-manager@0.1.6` 已通过 GitHub Actions OIDC 发布至 npm，`latest` 为 `0.1.6`。

## 已完成内容

中英文国际化（词典、t seat、label thunk）、业务与协议错误码化、状态译文不冻结、测试与交接文档收口。

## 待处理事项

- 本机 `0.1.6` 端到端验证（含中英文切换）转入交接入口待办。

## 关联提交

- `ebf7cd2` feat: add zh/en i18n via DSH locale service
- `931380b` fix: improve readonly error wording and harden error translation
- `eff1d08` chore: release version 0.1.6

## 阅读顺序

1. 本文件
2. `docs\03-技术架构\02-HTTP接口文档.md`
3. `docs\03-技术架构\01-技能存储与规范.md`
