# I024-读接口Host校验与信任边界 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU / Codex
- 优先级：高
- 更新时间：2026-08-17 02:07（Asia/Shanghai）

## 目标与业务边界

给 GET `/state` 补上与写接口一致的 loopback Host 校验，并写明 HTTP 接口只信任 loopback、import 可接受用户选定的本机路径。不改变启用时删除策略行的既有语义，不替换 `agent-preset/selected` 刷新斜杠菜单的现有实现。

## 任务清单

- [x] 抽出 `validateLoopbackHost`，在路由入口对全部方法生效。
- [x] GET `/state` 继续不要求请求标记和 JSON Content-Type。
- [x] 补充恶意 Host 与 localhost Host 的 GET 测试。
- [x] 在代码注释和接口文档中写明 YAML 顶层解析限制、启用删除策略行、loopback 信任边界。
- [x] 将官方 `skills/change` 替换借用事件登记为待办。

## 阻塞事项

无。

## 验收标准

- [x] 非 loopback Host 访问 GET `/state` 返回 403。
- [x] 正常客户端 GET `/state` 不带头仍返回 200。
- [x] `node test\core-test.mjs`：147 项通过。
- [x] `node test\locale-test.mjs`：449 项通过。

## 已完成内容

读接口与写接口统一校验 loopback Host；文档写明本接口信任 loopback，import 的 `source` 可以是用户选定的任意本机技能路径。

## 待处理事项

官方目录失效事件仍等宿主转发白名单，见交接入口待办。

## 关联提交

本机 I024 修复提交。

## 阅读顺序

1. 本文件
2. `docs\03-技术架构\02-HTTP接口文档.md`
3. `lib\index.js`
