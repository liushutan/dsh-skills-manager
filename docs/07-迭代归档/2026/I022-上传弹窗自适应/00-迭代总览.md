# I022-上传弹窗自适应 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU / Codex
- 优先级：高
- 更新时间：2026-08-16 17:05（Asia/Shanghai）

## 目标与业务边界

上传框是插件自有遮罩弹窗，应保持约 480px 宽、内容高度的居中卡片，不能铺满设置页。不改变上传、覆盖确认、删除确认的业务逻辑。

## 任务清单

- [x] 上传弹窗增加 `dssm-modal-upload`，强制 `480px` 宽和内容高度，覆盖宿主对话框拉伸。
- [x] 虚线投放区保持约 `144px` 高度，不随空白区域拉伸。
- [x] 窄屏下弹窗仍居中，宽度跟随遮罩内边距，按钮允许换行。
- [x] 覆盖/删除确认框保持内容高度，不被上传框的拉伸规则影响。
- [x] 补充客户端 CSS 约束测试。

## 阻塞事项

无。

## 验收标准

- [x] `node --check lib\client.js` 通过。
- [x] `node test\core-test.mjs`：136 项通过。
- [x] `node test\locale-test.mjs`：449 项通过。
- [ ] 真实 DSH 客户端中打开上传弹窗，确认为居中紧凑卡片，投放区和按钮完整可见。

## 已完成内容

上传弹窗改为居中紧凑卡片，避免铺满设置页；投放区不再被空白区域撑开。

## 待处理事项

真实 DSH 客户端视觉验收转入交接入口待办。

## 关联提交

- `481d557` fix: 上传弹窗随宿主区域自适应
- 紧凑尺寸修正与 `0.1.11` 发布提交

## 阅读顺序

1. 本文件
2. `docs\00-交接入口\02-当前状态.md`
3. `lib\client.js`