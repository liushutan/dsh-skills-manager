# I025-错误码国际化补齐 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU / Codex
- 优先级：中
- 更新时间：2026-08-17 02:55（Asia/Shanghai）

## 目标与业务边界

补齐 I024 之后漏出的错误码与译文：非法 Host、覆盖导入回滚失败、备份未清理。不改变日常上传/启停/删除流程，不新增功能入口。

## 任务清单

- [x] 非法 Host 与缺少写请求标记分码。
- [x] 覆盖导入回滚失败改为 `error.import.rollbackFailed`，不再抄 Node 系统码。
- [x] 备份未清理改为 `warning.backupUncleaned`，导入结果按当前语言展示。
- [x] 补齐 zh/en 词典、locale/core 测试与接口文档。

## 阻塞事项

无。

## 验收标准

- [x] 非 loopback Host 返回 `error.proto.forbiddenHost`；缺客户端标记仍为 `error.proto.forbidden`。
- [x] 回滚失败和备份警告可由词典翻译。
- [x] `node test\core-test.mjs`：147 项通过。
- [x] `node test\locale-test.mjs`：475 项通过。

## 已完成内容

Host/回滚/备份三条路径都带可翻译码；导入成功后的备份警告会追加到结果文案。

## 待处理事项

本机安装 `0.1.17` 后做中英文切换验收，见交接入口待办。

## 关联提交

本机 I025 修复提交与 `0.1.17` 发布提交。

## 阅读顺序

1. 本文件
2. `docs\03-技术架构\02-HTTP接口文档.md`
3. `lib\client.js`