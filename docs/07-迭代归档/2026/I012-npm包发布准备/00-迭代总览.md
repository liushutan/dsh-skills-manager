# I012-npm包发布准备 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-15 18:25（Asia/Shanghai）

## 目标与业务边界

将插件准备为公开 npm 包，使使用者可通过 DSH CLI 按包名安装，无需下载源码；不在未取得 npm 账号授权的环境中执行实际发布。

## 任务清单

- [x] 验证 DSH 支持 `dsh plugin --profile web add <包名>` 安装 npm bundle。
- [x] 补充 npm 仓库、问题追踪、主页和公开发布配置。
- [x] 将 README 使用的 Logo 与截图加入 tarball 白名单。
- [x] 添加发布前核心测试与 tarball 内容检查命令。
- [x] 更新中英文安装、发布和回滚说明。

## 验收标准

- [x] `npm pack --dry-run --json` 包含运行时代码、bundle patch 和 README 所用资源。
- [x] `npm test` 通过。
- [x] `dsh plugin --profile web add dsh-skills-manager` 成为推荐安装命令。

## 已完成内容

包名 `dsh-skills-manager` 查询时尚未被 npm 占用。发布需要在 npm 已登录且拥有该名称权限的环境中执行 `npm publish`；当前环境未登录，未执行发布。

## 待处理事项

维护者完成 `npm login` 后，先执行 `npm run pack:check`，再执行 `npm publish` 并在真实 DSH Web profile 中安装验证。

## 关联提交

`chore: prepare npm package distribution`

## 阅读顺序

1. `package.json`
2. `docs\05-工程交付\01-安装验证与发布.md`
3. 本文件
