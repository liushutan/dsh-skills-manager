# I009-停用调用与文件选择修正 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-15 17:13（Asia/Shanghai）

## 目标与业务边界

修复停用后仍显示在 `/` 手动调用列表、以及选择 `SKILL.md` 后自动弹出第二个选择窗口的问题。保持上传仍只写入 DSH 技能目录。

## 任务清单

- [x] 停用同时设置模型调用与手动调用禁用字段。
- [x] 启用移除两项禁用字段。
- [x] 无文件路径的文件选择只显示提示，不自动打开目录选择器。
- [x] 补充回归测试和更新使用文档。

## 验收标准

- [x] 停用后的 `SKILL.md` 包含 `user-invocable: false`。
- [x] 选择无路径的 `SKILL.md` 不触发目录选择服务。
- [x] `node test\core-test.mjs`：58 项通过。

## 已完成内容

“停用”现在表示同时禁用模型与 `/` 手动调用。若浏览器安全模型不提供所选文件的真实路径，上传框会说明原因，并等待用户主动选择插件文件夹。

## 待处理事项

真实 DSH profile 的重新加载与端到端验证仍见交接入口待办。

## 关联提交

待提交。

## 阅读顺序

1. 本文件
2. `docs\02-产品与业务\01-使用说明.md`
3. `lib\core.js`
