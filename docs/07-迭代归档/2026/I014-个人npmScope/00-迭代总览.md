# I014-个人npmScope · 迭代总览

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-15 18:55（Asia/Shanghai）

## 目标与业务边界

使用个人 npm 用户名 `michengai` 作为插件发布 scope，避免与 DeepSeek 官方 `@deepseek-ai` scope 混淆；不改变插件运行时接口。

## 任务清单

- [x] 将 npm 包名改为 `@michengai/dsh-skills-manager`。
- [x] 将 DSH bundle patch 的模块名改为完整 scoped 包名。
- [x] 更新中英文安装、发布和回滚命令。
- [x] 查询官方 npm registry，确认该 scoped 包尚未发布。

## 验收标准

- [x] `package.json` 与 `cordis.patch.yml` 使用一致的 scoped 包名。
- [x] README 推荐命令可直接按个人 scoped 包名安装。
- [x] tarball 可通过 npm 打包检查。

## 已完成内容

发布包名统一为 `@michengai/dsh-skills-manager`。`@deepseek-ai/*` 仅保留为 DSH peer dependency，不作为本插件发布主体。

## 待处理事项

维护者以 `michengai` 登录官方 npm registry 后，执行 `npm run pack:check`、`npm publish`，并在真实 DSH Web profile 验证安装。

## 关联提交

`chore: scope package to personal npm account`

## 阅读顺序

1. `package.json`
2. `cordis.patch.yml`
3. 本文件
