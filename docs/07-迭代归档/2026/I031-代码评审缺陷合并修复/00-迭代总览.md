# I031 代码评审缺陷合并修复

- 状态：已完成
- 负责人：YUJIYU
- 优先级：高
- 更新时间：2026-08-27 00:00（Asia/Shanghai）

## 目标与业务边界

合并两份代码评审，只修复经当前实现验证成立的缺陷；不在本迭代引入核心拆分、测试框架迁移、缓存或新依赖。

## 任务清单

- 修复删除过程二次失败时的唯一副本丢失窗口。
- 损坏的 `state.json` 改为 fail-closed，并禁止覆盖写入。
- 目录浏览增加 `lstat` 防护，不展示目录符号链接或 junction。
- 修正导入结果、警告展示与无效 invocation policy 的恢复入口。
- 补齐核心与本地化回归测试。

## 阻塞事项

无。

## 验收标准

- 删除回滚失败时保留 stage 并返回可诊断错误码。
- 已存在但不可读的状态文件不会重新启用外部技能，也不会被启停操作覆盖。
- 全跳过导入不显示成功；API 与导入警告可见；无效策略可一键修复并启用。
- 项目测试、打包检查、语法检查与差异检查全部通过。

## 已完成内容

- 核心安全缺陷、目录防护和 UI 反馈缺口均已修复。
- 新增确定性的删除双重失败、损坏状态、目录链接和客户端反馈回归用例。

## 待处理事项

无。后续可维护性建议见 `docs\00-交接入口\03-待办与阻塞.md`。

## 关联提交

- 本次修复提交（见 Git 历史）。

## 阅读顺序

1. 本文。
2. `docs\03-技术架构\02-HTTP接口文档.md`。
3. `docs\00-交接入口\02-当前状态.md`。
