# 当前状态

更新时间：2026-08-27 00:45（Asia/Shanghai）

## 当前阶段

`0.1.25` 已完成 I033 单次技能上传重构：导入弹窗统一支持 `.zip`、技能文件夹和单个 `SKILL.md`，浏览器读取内容后上传，不再暴露绝对路径或串联第二个目录选择器。上传内容在管理器私有目录暂存，经过路径、数量和大小边界校验后复用现有原子导入链路。

## 活动迭代

无。I033 已完成并归档，尚未提交或发布。

## 有效风险

- 斜杠菜单刷新仍借用宿主已转发的 `agent-preset/selected`；后续若 DSH 正式转发 `skills/change`，应切换为官方事件。
- 浏览器自动化端到端回归尚未纳入持续集成，当前依赖核心测试、本地化契约测试与本机 DSH Web 冒烟。
- `core.js` 仍集中承载多类核心职责；属于后续可维护性改进，不影响本次缺陷修复。
