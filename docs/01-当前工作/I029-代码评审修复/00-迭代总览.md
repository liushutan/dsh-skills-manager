# I029-代码评审修复 · 迭代总览

- 状态：已完成
- 负责人：YUJIYU / Claude
- 优先级：中
- 更新时间：2026-08-17 15:00（Asia/Shanghai）

## 目标与业务边界

落地 0.1.18 -> 0.1.20 代码评审提出的修复项：dry-run 预检与实际导入对符号链接口径一致、删除清理指向目录的伴随链接、HEAD 响应头与 GET 实体语义一致、上传失败清理已选来源、扫描复用技能根 realpath。不改协议路径、不改文案与错误码。

## 任务清单

- [x] analyzeSource / collectCandidates 改用 lstatOrNull，SKILL.md 本身是链接时 dry-run 即拒绝（error.source.symlink）。
- [x] dry-run 逐候选执行与实导相同的 assertNoSymbolicLinks：嵌套链接（如 scripts/foo）与目录超深（error.source.tooDeep）在预检阶段即结论，坏候选移入 failed、好候选保留 pending（评审 MEDIUM-1 完整收口）。
- [x] deleteSkill 伴随形态探测改用 lstatOrNull，链接形态一并纳入删除本体（fs.rm 不跟随链接）。
- [x] json()/run() 支持 headOnly，HEAD 与 GET/405 载荷的 content-length 一致，移除不精确的 allow 头。
- [x] executeImport 失败分支补 setSelected(null)，与成功路径一致。
- [x] 提取 isInsideResolvedRoot，scanEntries/resolveEntry 循环外缓存根 realpath。
- [x] 删除 importSkill dry-run 分支的死代码（新增预检后该分支重新承载剔除逻辑）。
- [x] 补齐回归测试（core-test 新增 10 项断言，symlink 用例在无链接权限环境自动跳过）。
- [x] 提交并随 0.1.22 发布（v0.1.21 因 CI 暴露 collectCandidates 吞错问题发布失败，tag 保留作记录，npm 上不存在该版本）。

## 阻塞事项

无。

## 验收标准

- [x] SKILL.md 为符号链接的来源在 dry-run 阶段返回 error.source.symlink（本机 Windows 无文件链接权限的用例在 CI Linux 生效）。
- [x] 含嵌套符号链接（nested-link 夹具）与超 64 层目录的来源，dry-run 即返回 ok:false + 对应错误码，不再出现「预检通过 -> 确认覆盖 -> 实导失败」。
- [x] 批量来源部分候选含链接时，dry-run 剔除坏候选进 failed、保留好候选在 pending。
- [x] HEAD /state 的 content-length 与 GET 实体一致，HEAD 405 与统一 405 载荷长度一致，body 均为空。
- [x] 删除 bundle 技能时，指向目录的伴随链接本体被清除，链接目标内容不受影响。
- [x] node test\core-test.mjs：190 项通过。
- [x] node test\locale-test.mjs：542 项通过。

## 已完成内容

首轮仅覆盖了分类阶段的 stat -> lstat（SKILL.md 本身是链接）；评审复核指出嵌套链接与深度超限仍只在实导阶段拦截，已补 preflightCandidates 逐候选预检，MEDIUM-1 完整收口。死代码清理、删除链接伴随、HEAD 语义、失败清理、realpath 缓存同步落地。全程 TDD（先失败后通过）。

## 待处理事项

无。发布后按「安装验证与发布」文档重装并实机过一遍上传/删除/启停。

## 关联提交

- e6aa6ff（首轮修复：dry-run 分类阶段口径、删除清理、HEAD 语义、失败清理、realpath 缓存）
- a0aac65（预检收口：dry-run 逐候选执行 assertNoSymbolicLinks）
- 149ed87 / v0.1.21（发布失败：CI Linux 暴露 batch 候选 symlink 拒绝被 try/catch 吞掉，npm 未发版）
- b7ec639（修复 collectCandidates 吞错）
- release 提交（版本 0.1.22 + 标签 v0.1.22，触发 OIDC 自动发布）

## 阅读顺序

1. 本文件
2. lib\core.js
3. lib\index.js
4. lib\client.js
5. test\core-test.mjs
